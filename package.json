{
  "name": "bb-chat",
  "version": "1.4.0",
  "private": true,
  "description": "BB Chat: a small polling-based multi-user chat app for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "pg": "^8.16.3"
  }
}
